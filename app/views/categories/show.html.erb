 <% @product = @category.products%>
 <%if @product.present?%>
   <% @product.each do |product| %>
      <%= link_to (image_tag product.image, size: "150x160"),product_path(product)%></a>
      <td><%= link_to 'Edit', edit_product_path(product)%></td>

      <td><%= link_to 'Destroy', product_path(product),
              method: :delete,
              data: { confirm: 'Are you sure?' } %></td></br>
      </br>
     <% end %>
   <% else %>
  < h1>No product available in this category </h1>
 <%end%>
