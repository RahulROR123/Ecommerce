<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <a class="navbar-brand" href="#">Ecommerce</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <a class="nav-link" href="/">Home <span class="sr-only">(current)</span></a>
      </li>
      <li class="nav-item">
         <% if current_user&.role == "user"%>
        <a class="nav-link" href="/carts">Cart_items</a>
        <%end%>
      </li>
      <li class="nav-item">
         <% if current_user&.role == "user"%>
        <a class="nav-link" href="/orders">orders_items</a>
        <%end%>
        <li class="nav-item">
         <% if current_user&.role == "user"%>
        <a class="nav-link" href="/categories">Category</a>
        <%end%>
      </li> 
      </li> 
    </ul>
    <form class="d-flex">
       <%=form_with url: "/",method: :get do |form| %>
        <%=form.text_field :name,:class=>"form-control mr-sm-2 " %>  
           <button class="btn btn-outline-success my-2 my-sm-0 " type="submit " style="margin: 6px;">Search</button>
         <%end%> 
    </from>
       <%if user_signed_in?%>     
             <button class="btn btn-outline-success  "> 
              <%= link_to 'Sign_out', destroy_user_session_path,:class => "some-class",method: :delete%>  </button>
                <%else%>
              <button class="btn btn-outline-success ">   
                <%=link_to "login",new_user_session_path %></button>
      <% end %>
  </div>
</nav>